<%= form_for(connections_categorylisting) do |f| %>
  <% if connections_categorylisting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(connections_categorylisting.errors.count, "error") %> prohibited this connections_categorylisting from being saved:</h2>

      <ul>
      <% connections_categorylisting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.text_field :category_id %>
  </div>

  <div class="field">
    <%= f.label :listing_id %>
    <%= f.text_field :listing_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
