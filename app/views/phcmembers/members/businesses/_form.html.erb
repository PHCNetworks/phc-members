<%= form_for(members_business) do |f| %>
  <% if members_business.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(members_business.errors.count, "error") %> prohibited this members_business from being saved:</h2>

      <ul>
      <% members_business.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
