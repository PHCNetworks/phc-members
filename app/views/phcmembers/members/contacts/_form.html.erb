<%= form_for(members_contact) do |f| %>
  <% if members_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(members_contact.errors.count, "error") %> prohibited this members_contact from being saved:</h2>

      <ul>
      <% members_contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
